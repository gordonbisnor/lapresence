.page-header
	.pull-right	
		.alert.alert-info Change item order by dragging and dropping them into place.
	%h1= t 'admin.ateliers.header'

= link_to t('admin.new_item'), new_admin_atelier_path, class: 'btn btn-default'

%hr

#ateliers.sortable{"data-target" => "/admin/ateliers/sort"}
	= render @items
