.page-header
	.pull-right	
		.alert.alert-info Change item order by dragging and dropping them into place.
	%h1
		<i class="far fa-handshake"></i>
		= t 'admin.ateliers.header'

= link_to  new_admin_atelier_path, class: 'btn btn-success' do
	<i class="fas fa-plus-circle"></i>
	= t('admin.new_item')

%hr

#ateliers.sortable{"data-target" => "/admin/ateliers/sort"}
	= render @items
