<div class="item <%= cycle("even","odd") %>" id="sort_item_<%= groupe.id %>">
	<div class="actions">
		<%= link_to "Edit", edit_admin_groupe_path(groupe) %> <%= link_to "Delete", admin_groupe_path(groupe), method: :delete, data: {confirm: "Are you sure?"} %>
	</div>
	<%= link_to groupe.title, groupe, target: "_blank" %> 
</div>