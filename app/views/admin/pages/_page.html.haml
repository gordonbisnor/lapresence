= div_for page, class: "item #{cycle("even","odd")}" do
	.pull-left.offset-right
		= link_to edit_button, edit_admin_page_path(page), title: "Edit", class: "edit-button"
	= link_to page.title, "/#{page.slug}", target: "_blank"
	%hr